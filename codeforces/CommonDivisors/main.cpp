#include<bits/stdc++.h>
using namespace std;
typedef long long int ll;
int main()
{
    string s1,s2;
    cin>>s1>>s2;
    int n=s1.size();
    int m=s2.size();
    if(n>m)
        swap(s1,s2);
    int i=0;
    int j=n/2;
    for (int k = 1; k <= n; ++k) {
        if(n%k==0)
        {
            a=n/k;
            for (int l = 0; l <a ; ++l) {
                if(s1[l]=s2[])
            }
        }
    }
    return 0;
}