import java.util.*;

class Solution {
    boolean[][] ans;

    public int countSubstrings(String s) {
        char[] str = s.toCharArray();
        int n = s.length();
        ans = new boolean[n][n];
        
    }
}